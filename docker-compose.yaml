services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: python-webapp-jenkins-1
    ports:
      - "8081:8080"
      - "50001:50000"
    volumes:
      - jenkins_home:/var/jenkins_home

volumes:
  jenkins_home: